TDD red:
- Local sync contract collection failed because scripts.configure_lakebase_sync did not exist.
- Live transaction test failed because schema student_retention_app did not exist.

Live green:
- RUN_LAKEBASE_INTEGRATION=1 pytest -q tests/integration/test_lakebase_transactions.py
- Result: 5 passed in 35.56s, including intervention transactions,
  immutability, serving-table reads, and SELECT-only reader-role privileges.

Final local verification:
- pytest -q: 112 passed, 5 skipped in 5.66s.
- python3 -m compileall -q src scripts tests: exit 0.
- databricks bundle validate --strict -t dev --profile fe-bar: Validation OK.

Remote CDC verification:
- Both intervention tables report CDF_STATE_STREAMING.
- Sanitized intervention STU-TASK8-ROUNDTRIP and its created event arrived in UC history.

Remote serving verification:
- Both synchronized tables report SYNCED_TABLE_ONLINE_NO_PENDING_UPDATE.
- They contain 15 rows each in Postgres schema student_retention_gold.
- The reader role has SELECT and lacks INSERT, UPDATE, and DELETE.
- STU-TASK8-ROUNDTRIP remained open at version 1 after both snapshots.
- No separate Lakebase catalog was created or required.
